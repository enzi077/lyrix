import { StyleSheet } from "react-native";

export const styles=StyleSheet.create({
    lyricsMatcher__subheading:{
        opacity: 0.6,
        marginBottom: 10
    },
    lyricsMatcher__lyricsBody:{
        opacity:0.8
    },
    lyricsMatcher__lyricsCopyright:{
        fontWeight: "bold"
    }
})